# Shell Script

**steps:**
* Create repo in github
* Clone in local laptop
* Start developing
* Add file to temp area
```
git add <file-name>
```
* Commit the changes
```
git commit -m "why you commited"
```
* Push the changes to github
```
git push -u origin main
```

**naming convention:small cases with hypen(-), no spaces**

